# Contributing

1. Fork and clone
2. `pip install -e '.[dev]'`
3. Make changes, add tests, run `pytest`
4. Open a pull request

## Good first issues
- Add PDF export using `weasyprint` (optional dependency)
- Add `--since` flag to filter messages by date
- Add support for exporting multiple threads in one run
